<Window x:Class="AstralNovastrom.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:AstralNovastrom.src.Controls"
        mc:Ignorable="d"
        Title="AstralNovastrom V5.0.0-Beta0.0.1"
        Height="800" Width="1600"
        WindowStartupLocation="CenterScreen"
        Background="#000000">

    <Grid>
        <!-- 主背景 -->
        <Grid.Background>
            <SolidColorBrush Color="#000000"/>
        </Grid.Background>

        <!-- 主容器 -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="400"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 左侧：曲速粒子反应处理装置 -->
            <controls:QuantumWaveformControl Grid.Column="0" Grid.Row="0" Margin="10"/>

            <!-- 右侧：数据稳定处理设备 -->
            <controls:SystemMonitoringControl Grid.Column="1" Grid.Row="0" Margin="10"/>

            <!-- 左侧下方：配置栏 -->
            <controls:ConfigurationPanelControl Grid.Column="0" Grid.Row="1" Margin="10"/>

            <!-- 右侧下方：火核处理器和日志 -->
            <controls:FireCoreProcessorControl Grid.Column="1" Grid.Row="1" Margin="10"/>
        </Grid>
    </Grid>
</Window>