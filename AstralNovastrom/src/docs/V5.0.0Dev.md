我将为你创建一个完整的项目结构。基于你的科幻设定和高级 Material Design 3 实现，这是一个合理的文件夹结构：

## 📁 项目完整结构

```
FrostShadowControl/
├── 📄 FrostShadowControl.csproj
├── 📄 App.xaml
├── 📄 App.xaml.cs
│
├── 📁 Views/
│   ├── 📄 MainWindow.xaml                    # 主界面
│   └── 📄 MainWindow.xaml.cs
│
├── 📁 ViewModels/
│   ├── 📄 MainViewModel.cs                   # 主视图模型
│   ├── 📄 LogViewModel.cs                    # 日志视图模型
│   ├── 📄 ReactorViewModel.cs                # 反应堆视图模型
│   ├── 📄 SecurityViewModel.cs               # 安全视图模型
│   └── 📄 ConfigViewModel.cs                 # 配置视图模型
│
├── 📁 Models/
│   ├── 📄 LogEntry.cs                        # 日志条目模型
│   ├── 📄 ReactorStatus.cs                   # 反应堆状态模型
│   ├── 📄 ProcessorMetrics.cs                # 处理器指标模型
│   ├── 📄 SecurityAlert.cs                   # 安全警报模型
│   └── 📄 SystemConfig.cs                    # 系统配置模型
│
├── 📁 Services/
│   ├── 📁 Reactor/
│   │   ├── 📄 ReactorSimulationService.cs    # 反应堆模拟服务
│   │   ├── 📄 ParticleFlowService.cs         # 粒子流服务
│   │   └── 📄 EnergyExtractionService.cs     # 能量提取服务
│   │
│   ├── 📁 Security/
│   │   ├── 📄 ThreatDetectionService.cs      # 威胁检测服务
│   │   ├── 📄 HackFrameworkService.cs        # Hack框架服务
│   │   └── 📄 FirewallService.cs             # 防火墙服务
│   │
│   ├── 📁 Processor/
│   │   ├── 📄 FireCoreProcessorService.cs    # 火核处理器服务
│   │   ├── 📄 CoolingSystemService.cs        # 冷却系统服务
│   │   └── 📄 LoadBalancingService.cs        # 负载均衡服务
│   │
│   ├── 📁 Monitoring/
│   │   ├── 📄 LogService.cs                  # 日志服务
│   │   ├── 📄 MetricsCollectorService.cs     # 指标收集服务
│   │   └── 📄 AlertService.cs                # 警报服务
│   │
│   └── 📄 SimulationService.cs               # 整体模拟服务
│
├── 📁 Components/
│   ├── 📁 Controls/
│   │   ├── 📄 CyberProgressBar.xaml          # 自定义进度条
│   │   ├── 📄 WaveformChart.xaml             # 波形图控件
│   │   ├── 📄 StatusIndicator.xaml           # 状态指示灯
│   │   ├── 📄 DataCard.xaml                  # 数据卡片控件
│   │   └── 📄 LogViewer.xaml                 # 日志查看器
│   │
│   ├── 📁 Dialogs/
│   │   ├── 📄 ConfigDialog.xaml              # 配置对话框
│   │   ├── 📄 AlertDialog.xaml               # 警报对话框
│   │   ├── 📄 EmergencyProtocolDialog.xaml   # 紧急协议对话框
│   │   └── 📄 HackFrameworkDialog.xaml       # Hack框架对话框
│   │
│   └── 📁 Panels/
│       ├── 📄 ReactorControlPanel.xaml       # 反应堆控制面板
│       ├── 📄 SecurityPanel.xaml             # 安全面板
│       ├── 📄 MonitoringPanel.xaml           # 监控面板
│       └── 📄 ConfigurationPanel.xaml        # 配置面板
│
├── 📁 Converters/
│   ├── 📄 LogStyleConverter.cs               # 日志样式转换器
│   ├── 📄 ThreatLevelConverter.cs            # 威胁级别转换器
│   ├── 📄 EfficiencyColorConverter.cs        # 效率颜色转换器
│   ├── 📄 TemperatureColorConverter.cs       # 温度颜色转换器
│   └── 📄 CornerRadiusConverter.cs           # 圆角转换器
│
├── 📁 Utilities/
│   ├── 📄 AnimationHelper.cs                 # 动画助手
│   ├── 📄 ColorHelper.cs                     # 颜色助手
│   ├── 📄 CyberTextGenerator.cs              # 乱码文本生成器
│   ├── 📄 WaveformGenerator.cs               # 波形生成器
│   └── 📄 Extensions.cs                      # 扩展方法
│
├── 📁 Resources/
│   ├── 📁 Styles/
│   │   ├── 📄 MaterialDesign3Override.xaml   # Material Design 3 覆盖样式
│   │   ├── 📄 CyberStyles.xaml               # 科幻样式
│   │   ├── 📄 Animations.xaml                # 动画资源
│   │   └── 📄 Icons.xaml                     # 图标资源
│   │
│   ├── 📁 Brushes/
│   │   ├── 📄 QuantumBrushes.xaml            # 量子渐变画刷
│   │   └── 📄 HoloBrushes.xaml               # 全息画刷
│   │
│   └── 📁 Data/
│       └── 📄 AttackPatterns.json            # 攻击模式数据
│
└── 📁 Assets/
    ├── 📁 Icons/
    ├── 📁 Sounds/
    └── 📁 Fonts/
```

## 现在，让我们创建关键文件：

### 1. **项目文件 (FrostShadowControl.csproj)**

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <UseWPF>true</UseWPF>
    <ApplicationManifest>app.manifest</ApplicationManifest>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="MaterialDesignThemes" Version="5.3.0" />
    <PackageReference Include="MaterialDesignColors" Version="2.0.0" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.2.1" />
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="8.0.0" />
  </ItemGroup>

  <ItemGroup>
    <Resource Include="Assets\**">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Resource>
  </ItemGroup>
</Project>
```

### 2. **应用配置文件 (App.xaml)**

```xml
<Application x:Class="FrostShadowControl.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FrostShadowControl"
             StartupUri="Views/MainWindow.xaml">
  <Application.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <!-- Material Design 3 -->
        <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Light.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
        
        <!-- 自定义资源 -->
        <ResourceDictionary Source="Resources/Styles/MaterialDesign3Override.xaml" />
        <ResourceDictionary Source="Resources/Styles/CyberStyles.xaml" />
        <ResourceDictionary Source="Resources/Styles/Animations.xaml" />
        <ResourceDictionary Source="Resources/Brushes/QuantumBrushes.xaml" />
        <ResourceDictionary Source="Resources/Brushes/HoloBrushes.xaml" />
        
        <!-- 视图模型定位器 -->
        <ResourceDictionary Source="ViewModels/ViewModelLocator.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Application.Resources>
</Application>
```

### 3. **资源文件创建**

#### **Resources/Styles/MaterialDesign3Override.xaml**
```xml
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- Material Design 3 主题覆盖 -->
  
  <!-- 颜色主题 -->
  <Color x:Key="PrimaryColor">#FF00C6FF</Color>
  <Color x:Key="SecondaryColor">#FF9D00FF</Color>
  <Color x:Key="TertiaryColor">#FF00FFAA</Color>
  <Color x:Key="ErrorColor">#FFFF5555</Color>
  <Color x:Key="WarningColor">#FFFFAA00</Color>
  <Color x:Key="SuccessColor">#FF00FF88</Color>
  
  <!-- 表面颜色 -->
  <Color x:Key="SurfaceColor">#FF0A0A14</Color>
  <Color x:Key="SurfaceVariantColor">#FF1A1A28</Color>
  <Color x:Key="OutlineColor">#FF3A3A5A</Color>
  
  <!-- 文本颜色 -->
  <Color x:Key="OnPrimaryColor">#FF000000</Color>
  <Color x:Key="OnSecondaryColor">#FF000000</Color>
  <Color x:Key="OnSurfaceColor">#FFE0E0FF</Color>
  
  <!-- 画刷 -->
  <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
  <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
  <SolidColorBrush x:Key="TertiaryBrush" Color="{StaticResource TertiaryColor}"/>
  <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
  <SolidColorBrush x:Key="SurfaceVariantBrush" Color="{StaticResource SurfaceVariantColor}"/>
  <SolidColorBrush x:Key="OnSurfaceBrush" Color="{StaticResource OnSurfaceColor}"/>
  
  <!-- 字体 -->
  <FontFamily x:Key="CyberDisplayFont">JetBrains Mono, Consolas, Cascadia Code</FontFamily>
  <FontFamily x:Key="CyberBodyFont">Segoe UI Variable, Segoe UI</FontFamily>
  
  <!-- 形状 -->
  <CornerRadius x:Key="ShapeExtraSmall">4</CornerRadius>
  <CornerRadius x:Key="ShapeSmall">8</CornerRadius>
  <CornerRadius x:Key="ShapeMedium">12</CornerRadius>
  <CornerRadius x:Key="ShapeLarge">16</CornerRadius>
  <CornerRadius x:Key="ShapeExtraLarge">24</CornerRadius>
  <CornerRadius x:Key="ShapeFull">9999</CornerRadius>
  
  <!-- 按钮样式 -->
  <Style x:Key="MaterialDesignFilledButton" TargetType="Button">
    <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
    <Setter Property="Foreground" Value="{StaticResource OnPrimaryBrush}"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="24,12"/>
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="FontWeight" Value="Medium"/>
    <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="{StaticResource ShapeFull}"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Grid>
            <Border x:Name="BackgroundBorder"
                    Background="{TemplateBinding Background}"
                    CornerRadius="{TemplateBinding materialDesign:ButtonAssist.CornerRadius}">
              <ContentPresenter x:Name="ContentPresenter"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Margin="{TemplateBinding Padding}"/>
            </Border>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="BackgroundBorder" Property="Opacity" Value="0.8"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="BackgroundBorder" Property="Opacity" Value="0.6"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
```

#### **Resources/Styles/CyberStyles.xaml**
```xml
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:FrostShadowControl.Converters">
  
  <!-- 转换器 -->
  <converters:LogStyleConverter x:Key="LogStyleConverter"/>
  <converters:ThreatLevelConverter x:Key="ThreatLevelConverter"/>
  <converters:EfficiencyColorConverter x:Key="EfficiencyColorConverter"/>
  <converters:TemperatureColorConverter x:Key="TemperatureColorConverter"/>
  <converters:CornerRadiusConverter x:Key="CornerRadiusConverter"/>
  
  <!-- 科幻卡片样式 -->
  <Style x:Key="CyberCard" TargetType="Border">
    <Setter Property="Background" Value="{StaticResource SurfaceVariantBrush}"/>
    <Setter Property="CornerRadius" Value="{StaticResource ShapeLarge}"/>
    <Setter Property="BorderBrush" Value="{StaticResource OutlineBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Effect">
      <Setter.Value>
        <DropShadowEffect Color="#40000000" BlurRadius="16" ShadowDepth="2"/>
      </Setter.Value>
    </Setter>
  </Style>
  
  <!-- 状态指示灯 -->
  <Style x:Key="StatusIndicatorStyle" TargetType="Ellipse">
    <Setter Property="Width" Value="12"/>
    <Setter Property="Height" Value="12"/>
    <Setter Property="Stroke" Value="{StaticResource OutlineBrush}"/>
    <Setter Property="StrokeThickness" Value="1"/>
  </Style>
  
  <!-- 数据值显示 -->
  <Style x:Key="DataValueStyle" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource CyberDisplayFont}"/>
    <Setter Property="FontSize" Value="24"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
  </Style>
  
  <!-- 标签样式 -->
  <Style x:Key="DataLabelStyle" TargetType="TextBlock">
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Foreground" Value="#AAAAAA"/>
    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
  </Style>
  
  <!-- 日志条目样式 -->
  <Style x:Key="LogEntryBaseStyle" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource CyberDisplayFont}"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
  </Style>
  
  <Style x:Key="LogInfoStyle" TargetType="TextBlock" BasedOn="{StaticResource LogEntryBaseStyle}">
    <Setter Property="Foreground" Value="{StaticResource SuccessColor}"/>
  </Style>
  
  <Style x:Key="LogWarningStyle" TargetType="TextBlock" BasedOn="{StaticResource LogEntryBaseStyle}">
    <Setter Property="Foreground" Value="{StaticResource WarningColor}"/>
    <Setter Property="Effect">
      <Setter.Value>
        <DropShadowEffect Color="#80FFAA00" BlurRadius="10" ShadowDepth="0"/>
      </Setter.Value>
    </Setter>
  </Style>
  
  <Style x:Key="LogCriticalStyle" TargetType="TextBlock" BasedOn="{StaticResource LogEntryBaseStyle}">
    <Setter Property="Foreground" Value="{StaticResource ErrorColor}"/>
    <Setter Property="Effect">
      <Setter.Value>
        <DropShadowEffect Color="#80FF5555" BlurRadius="15" ShadowDepth="0"/>
      </Setter.Value>
    </Setter>
  </Style>
  
</ResourceDictionary>
```

#### **Resources/Brushes/QuantumBrushes.xaml**
```xml
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  
  <!-- 量子渐变画刷 -->
  <LinearGradientBrush x:Key="QuantumGradientBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="#FF00C6FF" Offset="0"/>
    <GradientStop Color="#FF9D00FF" Offset="0.5"/>
    <GradientStop Color="#FF00FFAA" Offset="1"/>
  </LinearGradientBrush>
  
  <!-- 全息效果 -->
  <LinearGradientBrush x:Key="HoloGradientBrush" StartPoint="0,0" EndPoint="1,0">
    <GradientStop Color="#2000C6FF" Offset="0"/>
    <GradientStop Color="#409D00FF" Offset="0.5"/>
    <GradientStop Color="#2000C6FF" Offset="1"/>
  </LinearGradientBrush>
  
  <!-- 效率渐变 -->
  <LinearGradientBrush x:Key="EfficiencyGradientBrush" StartPoint="0,0" EndPoint="1,0">
    <GradientStop Color="#FF00FF88" Offset="0"/>
    <GradientStop Color="#FFFFFF00" Offset="0.7"/>
    <GradientStop Color="#FFFF0000" Offset="1"/>
  </LinearGradientBrush>
  
</ResourceDictionary>
```

### 4. **模型类创建**

#### **Models/LogEntry.cs**
```csharp
using System;
using System.ComponentModel;

namespace FrostShadowControl.Models
{
    public enum LogType
    {
        Info,
        Warning,
        Critical
    }

    public class LogEntry : INotifyPropertyChanged
    {
        private string _message;
        private bool _isGlitching;
        private LogType _type;
        private DateTime _timestamp;

        public string Message
        {
            get => _message;
            set
            {
                if (_message != value)
                {
                    _message = value;
                    OnPropertyChanged(nameof(Message));
                }
            }
        }

        public LogType Type
        {
            get => _type;
            set
            {
                if (_type != value)
                {
                    _type = value;
                    OnPropertyChanged(nameof(Type));
                }
            }
        }

        public DateTime Timestamp
        {
            get => _timestamp;
            set
            {
                if (_timestamp != value)
                {
                    _timestamp = value;
                    OnPropertyChanged(nameof(Timestamp));
                }
            }
        }

        public bool IsGlitching
        {
            get => _isGlitching;
            set
            {
                if (_isGlitching != value)
                {
                    _isGlitching = value;
                    OnPropertyChanged(nameof(IsGlitching));
                }
            }
        }

        public event PropertyChangedEventHandler PropertyChanged;

        protected virtual void OnPropertyChanged(string propertyName)
        {
            PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(propertyName));
        }

        public override string ToString()
        {
            return $"[{Timestamp:HH:mm:ss}] {Message}";
        }
    }
}
```

#### **Models/ReactorStatus.cs**
```csharp
using System.ComponentModel;

namespace FrostShadowControl.Models
{
    public class ReactorStatus : INotifyPropertyChanged
    {
        private double _psiStability = 7.2;
        private double _extractionEfficiency = 87.2;
        private double _particleFlowRate = 320.5;
        private double _energyOutput = 1500.0;
        private bool _isStable = true;
        private string _currentMode = "Normal";

        public double PsiStability
        {
            get => _psiStability;
            set
            {
                if (_psiStability != value)
                {
                    _psiStability = value;
                    OnPropertyChanged(nameof(PsiStability));
                }
            }
        }

        public double ExtractionEfficiency
        {
            get => _extractionEfficiency;
            set
            {
                if (_extractionEfficiency != value)
                {
                    _extractionEfficiency = value;
                    OnPropertyChanged(nameof(ExtractionEfficiency));
                }
            }
        }

        public double ParticleFlowRate
        {
            get => _particleFlowRate;
            set
            {
                if (_particleFlowRate != value)
                {
                    _particleFlowRate = value;
                    OnPropertyChanged(nameof(ParticleFlowRate));
                }
            }
        }

        public double EnergyOutput
        {
            get => _energyOutput;
            set
            {
                if (_energyOutput != value)
                {
                    _energyOutput = value;
                    OnPropertyChanged(nameof(EnergyOutput));
                }
            }
        }

        public bool IsStable
        {
            get => _isStable;
            set
            {
                if (_isStable != value)
                {
                    _isStable = value;
                    OnPropertyChanged(nameof(IsStable));
                }
            }
        }

        public string CurrentMode
        {
            get => _currentMode;
            set
            {
                if (_currentMode != value)
                {
                    _currentMode = value;
                    OnPropertyChanged(nameof(CurrentMode));
                }
            }
        }

        public event PropertyChangedEventHandler PropertyChanged;

        protected virtual void OnPropertyChanged(string propertyName)
        {
            PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(propertyName));
        }
    }
}
```

#### **Models/ProcessorMetrics.cs**
```csharp
using System.ComponentModel;

namespace FrostShadowControl.Models
{
    public class ProcessorMetrics : INotifyPropertyChanged
    {
        private double _loadPercentage = 47.0;
        private double _temperature = 42.0;
        private double _memoryIntegrity = 99.7;
        private double _instructionThroughput = 12.5;
        private bool _isOverheating = false;
        private string _status = "Normal";

        public double LoadPercentage
        {
            get => _loadPercentage;
            set
            {
                if (_loadPercentage != value)
                {
                    _loadPercentage = value;
                    OnPropertyChanged(nameof(LoadPercentage));
                    
                    // 更新状态
                    UpdateStatus();
                }
            }
        }

        public double Temperature
        {
            get => _temperature;
            set
            {
                if (_temperature != value)
                {
                    _temperature = value;
                    OnPropertyChanged(nameof(Temperature));
                    
                    // 检查是否过热
                    IsOverheating = _temperature > 120;
                }
            }
        }

        public double MemoryIntegrity
        {
            get => _memoryIntegrity;
            set
            {
                if (_memoryIntegrity != value)
                {
                    _memoryIntegrity = value;
                    OnPropertyChanged(nameof(MemoryIntegrity));
                }
            }
        }

        public double InstructionThroughput
        {
            get => _instructionThroughput;
            set
            {
                if (_instructionThroughput != value)
                {
                    _instructionThroughput = value;
                    OnPropertyChanged(nameof(InstructionThroughput));
                }
            }
        }

        public bool IsOverheating
        {
            get => _isOverheating;
            private set
            {
                if (_isOverheating != value)
                {
                    _isOverheating = value;
                    OnPropertyChanged(nameof(IsOverheating));
                }
            }
        }

        public string Status
        {
            get => _status;
            private set
            {
                if (_status != value)
                {
                    _status = value;
                    OnPropertyChanged(nameof(Status));
                }
            }
        }

        private void UpdateStatus()
        {
            if (_loadPercentage >= 100)
            {
                Status = "Critical";
            }
            else if (_loadPercentage >= 90)
            {
                Status = "Warning";
            }
            else if (_loadPercentage >= 70)
            {
                Status = "High";
            }
            else
            {
                Status = "Normal";
            }
        }

        public event PropertyChangedEventHandler PropertyChanged;

        protected virtual void OnPropertyChanged(string propertyName)
        {
            PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(propertyName));
        }
    }
}
```

### 5. **转换器创建**

#### **Converters/LogStyleConverter.cs**
```csharp
using System;
using System.Windows;
using System.Windows.Data;
using FrostShadowControl.Models;

namespace FrostShadowControl.Converters
{
    public class LogStyleConverter : IValueConverter
    {
        public object Convert(object value, Type targetType, object parameter, System.Globalization.CultureInfo culture)
        {
            if (value is LogType logType)
            {
                return logType switch
                {
                    LogType.Info => Application.Current.FindResource("LogInfoStyle"),
                    LogType.Warning => Application.Current.FindResource("LogWarningStyle"),
                    LogType.Critical => Application.Current.FindResource("LogCriticalStyle"),
                    _ => Application.Current.FindResource("LogInfoStyle")
                };
            }
            return Application.Current.FindResource("LogInfoStyle");
        }

        public object ConvertBack(object value, Type targetType, object parameter, System.Globalization.CultureInfo culture)
        {
            throw new NotImplementedException();
        }
    }
}
```

#### **Converters/EfficiencyColorConverter.cs**
```csharp
using System;
using System.Windows.Data;
using System.Windows.Media;

namespace FrostShadowControl.Converters
{
    public class EfficiencyColorConverter : IValueConverter
    {
        public object Convert(object value, Type targetType, object parameter, System.Globalization.CultureInfo culture)
        {
            if (value is double efficiency)
            {
                if (efficiency >= 95) return new SolidColorBrush(Color.FromArgb(255, 255, 85, 85)); // 红色
                if (efficiency >= 90) return new SolidColorBrush(Color.FromArgb(255, 255, 170, 0)); // 橙色
                if (efficiency >= 80) return new SolidColorBrush(Color.FromArgb(255, 255, 255, 0)); // 黄色
                if (efficiency >= 70) return new SolidColorBrush(Color.FromArgb(255, 170, 255, 0)); // 黄绿色
                return new SolidColorBrush(Color.FromArgb(255, 0, 255, 136)); // 绿色
            }
            return Brushes.Gray;
        }

        public object ConvertBack(object value, Type targetType, object parameter, System.Globalization.CultureInfo culture)
        {
            throw new NotImplementedException();
        }
    }
}
```

### 6. **主窗口简化版 (Views/MainWindow.xaml)**

```xml
<Window x:Class="FrostShadowControl.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.microsoft.com/openschema/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="霜影核能控制台" 
        Height="900" Width="1600"
        WindowStartupLocation="CenterScreen">
    
    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF0A0A14" Offset="0"/>
            <GradientStop Color="#FF0A0A1A" Offset="0.5"/>
            <GradientStop Color="#FF140A1A" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    
    <Grid>
        <!-- 暂时简化布局 -->
        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 左侧：标题 -->
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="霜影核能控制台" 
                           FontSize="32" FontWeight="Bold"
                           Foreground="#FF00C6FF"/>
                <TextBlock Text="5067 · 量子提取系统" 
                           FontSize="16"
                           Foreground="#FF9D00FF"
                           Margin="0,10,0,0"/>
            </StackPanel>
            
            <!-- 右侧：状态 -->
            <StackPanel Grid.Column="1" HorizontalAlignment="Right" 
                        VerticalAlignment="Center">
                <Border Background="#201A1A28" CornerRadius="8" Padding="12">
                    <StackPanel>
                        <TextBlock Text="系统状态" 
                                   Foreground="#AAAAAA"
                                   FontSize="12"/>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <Ellipse Width="12" Height="12" Fill="#FF00FF88"
                                     Margin="0,0,8,0"/>
                            <TextBlock Text="在线" 
                                       Foreground="#FF00FF88"
                                       FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
```

### 7. **创建文件夹的批处理脚本**

创建 `create_structure.bat` 文件：

```batch
@echo off
echo Creating FrostShadowControl project structure...

:: 创建根目录
mkdir FrostShadowControl
cd FrostShadowControl

:: 创建主要文件夹
mkdir Views
mkdir ViewModels
mkdir Models
mkdir Services
mkdir Services\Reactor
mkdir Services\Security
mkdir Services\Processor
mkdir Services\Monitoring
mkdir Components
mkdir Components\Controls
mkdir Components\Dialogs
mkdir Components\Panels
mkdir Converters
mkdir Utilities
mkdir Resources
mkdir Resources\Styles
mkdir Resources\Brushes
mkdir Resources\Data
mkdir Assets
mkdir Assets\Icons
mkdir Assets\Sounds
mkdir Assets\Fonts

echo Structure created successfully!
pause
```

## 创建步骤：

1. **运行批处理脚本**创建所有文件夹
2. **复制以上代码**到对应的文件中
3. **安装 NuGet 包**：
   ```bash
   dotnet add package MaterialDesignThemes --version 5.3.0
   dotnet add package MaterialDesignColors --version 2.0.0
   dotnet add package CommunityToolkit.Mvvm --version 8.2.1
   ```

4. **从最简单的开始**：
   - 先创建基本项目结构
   - 实现 MainWindow 和基础模型
   - 逐步添加服务和组件

这样结构化的组织方式可以让你：
- **模块化开发**：每个功能都在自己的文件夹中
- **易于维护**：清晰的职责分离
- **便于扩展**：可以轻松添加新功能
- **团队协作友好**：标准化的项目结构

建议你从这个简化版本开始，逐步添加高级功能。需要我继续为其他部分提供代码吗？