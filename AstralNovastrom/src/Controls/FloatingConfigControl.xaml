<UserControl x:Class="AstralNovastrom.src.Controls.FloatingConfigControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Border Background="#800A0A14">
        <materialDesign:Card Width="400" Height="300" 
                             UniformCornerRadius="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 标题 -->
            <Border Grid.Row="0" 
                    Padding="20,16,20,12"
                    BorderBrush="{StaticResource Outline}"
                    BorderThickness="0,0,0,1">
                <TextBlock Text="系统配置" 
                           FontSize="16" FontWeight="Bold"
                           Foreground="{StaticResource OnSurface}"/>
            </Border>

            <!-- 配置内容 -->
            <Grid Grid.Row="1" Margin="20">
                <StackPanel>
                    <TextBlock Text="安全设置" 
                               Style="{StaticResource ValueLabel}"
                               Margin="0,0,0,12"/>
                    <CheckBox Content="启用自动超载保护" 
                              Foreground="{StaticResource OnSurface}"
                              IsChecked="True"
                              Margin="0,4"/>
                    <CheckBox Content="启用量子入侵检测" 
                              Foreground="{StaticResource OnSurface}"
                              IsChecked="True"
                              Margin="0,4"/>
                    <CheckBox Content="启用实时威胁分析" 
                              Foreground="{StaticResource OnSurface}"
                              IsChecked="True"
                              Margin="0,4"/>
                </StackPanel>
            </Grid>

                <!-- 按钮 -->
                <StackPanel Grid.Row="2" 
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="20,12">
                    <Button Style="{StaticResource OutlinedButton}" 
                            Content="取消"
                            Margin="0,0,8,0"/>
                    <Button Style="{StaticResource FilledButton}" 
                            Content="保存"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Border>
</UserControl>
