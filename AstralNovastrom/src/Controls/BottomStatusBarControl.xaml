<UserControl x:Class="AstralNovastrom.src.Controls.BottomStatusBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="48" d:DesignWidth="800">
    <Border Background="{StaticResource SurfaceVariant}"
            CornerRadius="{StaticResource ExtraLarge}"
            Height="48">
        <Grid Margin="24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：状态-->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="系统状态 在线" 
                           Style="{StaticResource ValueLabel}"
                           Margin="0,0,16,0"/>
                <TextBlock Text="连接数 1,247" 
                           Style="{StaticResource ValueLabel}"
                           Margin="0,0,16,0"/>
                <TextBlock Text="安全等级: 高" 
                           Style="{StaticResource ValueLabel}"/>
            </StackPanel>

            <!-- 右侧：版本-->
            <TextBlock Grid.Column="1" 
                       Text="v5.0.1" 
                       Style="{StaticResource ValueLabel}"
                       VerticalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>
